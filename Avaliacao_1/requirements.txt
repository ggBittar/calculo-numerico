numpy>=1.24
matplotlib>=3.7

# Opcional: para usar GPU com CUDA no solver da avaliacao.
# Importante: use o wheel binario `cupy-cuda12x` e nao `cupy`,
# para evitar compilacao local. O extra `[ctk]` instala os componentes
# CUDA via PyPI, reduzindo a dependencia de um CUDA Toolkit local.
cupy-cuda12x[ctk]>=13.0 ; platform_system == "Windows"
cupy-cuda12x[ctk]>=13.0 ; platform_system == "Linux"
